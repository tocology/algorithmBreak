#include <cstdio>
#include <vector>

using namespace std;

int cache[10000][1000]; // Remain weight, Used Jewels
int N, W;
vector<pair<int, int> > jewels;


int main(){
      
      scanf("%d %d", &N, &W);
        
        jewels = vector<pair<int, int> >(N);
          
          for(int i=0; i<N; ++i){
                  int weight, price;
                      scanf("%d %d", &weight, &price);
                          jewels[i] =
                                }
            
            
            return 0;
}
